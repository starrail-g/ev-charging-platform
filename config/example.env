# 复制为 config/local.env（已被 .gitignore）后按需填写：
#   Copy-Item config/example.env config/local.env   (Windows)
#   cp config/example.env config/local.env          (Ubuntu)
# local.env 与真实密钥永不进入 Git、日志、截图或测试快照。

EV_SERVER_HOST=127.0.0.1
EV_SERVER_PORT=45454
EV_DATABASE_PATH=var/ev-charging.db
EV_SCHEMA_PATH=database/schema/schema.sql
EV_DATABASE_SEED_PATH=database/seeds/dev.sql

# 腾讯地图服务端开发配置（本地专用，永不提交真实凭据）：
# 仅 B 服务端读取 TENCENT_MAP_KEY；用户端通过 Socket 获取站点和路线，不读取此 Key。
# TENCENT_MAP_ENABLED=0 可强制服务端走确定性的 Mock/缓存降级路径。
TENCENT_MAP_KEY=
TENCENT_MAP_ENABLED=1
# TENCENT_MAP_BASE_URL 留空使用官方 https://apis.map.qq.com；仅 fake HTTP 测试覆盖。
# TENCENT_MAP_TIMEOUT_MS=5000

# 用户端腾讯 JavaScript API GL 底图 Key（仅 apps/user-client 使用）：
# 该 Key 只负责底图和服务端所返坐标/折线的可视化，不调用 POI、地理编码或路线接口。
# 它与服务端 WebService Key 分离，留空时用户端显示离线网格预览。
TENCENT_MAP_JS_KEY=

# 管理端静态底图配置（仅 apps/admin-client 使用）：
# 类型为腾讯 WebServiceAPI Key，必须使用 IP 白名单。桌面请求没有 Referer，
# 使用域名白名单会返回 status 110；未绑定配额方案可能返回 status 121。
# 此 Key 与服务端 TENCENT_MAP_KEY 分离，留空时管理端自动显示本地拓扑。
# 真实 Key 只放 local.env 或运行环境，不进入日志、截图、数据库或 Git。
TENCENT_STATIC_MAP_KEY=

# 本地演示服务端口（dashboard/serve.py 默认 8080，可用 --port 覆盖）。
DASHBOARD_PORT=8080

# 管理端数据源切换（apps/admin-client，接口闸门联调用）：
#   EV_ADMIN_DATA_SOURCE=socket → SocketAdminRepository（连 EV_SERVER_HOST:PORT）
#   空/其他值 → Mock 演示（默认；9/7 18:00 闸门前不设此项，材料不冒充真实联调）。
EV_ADMIN_DATA_SOURCE=
