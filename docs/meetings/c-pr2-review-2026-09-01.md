# 同学 C PR #2 复核记录

**复核日期**：2026-09-01
**PR**：[starrail-g/ev-charging-platform#2](https://github.com/starrail-g/ev-charging-platform/pull/2)
**PR 标题**：`feat(admin-client): establish qmake project skeleton with five-page navigation and smoke tests T-C0.1`
**Base**：`main`（`ec3b6986042e065b99e8332ecfdc5ae1cf8d4ba4`）
**Head**：`1d03edf3e3bc02b52645d88d1c02cced9a383923`
**状态**：OPEN，6 个提交，28 个文件。

## 1. 复核范围与方法

- 通过 GitHub PR 补丁和 head 提交读取实际变更，而不是只依据 PR 描述。
- 在隔离 worktree 中检查文件清单、源码、qmake 工程、文档、测试声明和 `git diff --check`。
- 对照仓库 `AGENTS.md`、`CONTRIBUTING.md`、PR head 中的 `current.md`，以及本项目 A-S1-01 权威需求矩阵 `docs/requirements/requirements-matrix.md`。
- 当前复核环境没有 PR README 声明的 Qt/qmake 工具链，因此未伪造独立构建通过；PR 描述中的构建结果视为提交者声明，仓库内没有对应日志或报告。

## 2. PR 实际包含内容

### 已包含

- `apps/admin-client` 的 qmake `subdirs` 工程、最小 Qt Widgets 主窗口和登录/概览/桩/站点/用户五页占位页面。
- 登录页到业务区、未登录导航锁定、退出后返回登录页的最小状态切换。
- `tst_launchsmoke.cpp` 两个测试槽位；Qt Test 自动计入初始化/清理后，PR 描述的 4 个通过项在数量上可以成立。
- 管理端架构说明、API 依赖说明、C 角色需求追溯表、UI 说明、缺陷日志和协作/发布计划的仓库版文档。
- `.gitignore` 对构建产物、运行数据库、`.env` 和 `docs/superpowers/` 的排除规则。
- `git diff --check` 通过；未发现已跟踪的数据库文件或明显密钥模式。

### 未包含

- 管理端 `AdminRepository`、`MockAdminRepository`、`SocketAdminRepository` 和任何数据模型/适配器。
- 概览、桩、站点、用户页面的真实或 Mock 数据展示、筛选、刷新、异常态和操作逻辑。
- `tst_loginflow.cpp`、`tst_mockrepository.cpp`、`tst_statusmapping.cpp`。
- `dashboard/index.html`、离线入口、ECharts 本地文件、演示 JSON 和 dashboard README。
- 接口闸门记录、集成冒烟文档、回归记录、第一/第二阶段发布清单。
- PR 对 `current.md` 的任何更新。

## 3. 必须整改的问题

### P1-01：代码/架构变更未同步 `current.md`

PR 增加了管理端工程、页面边界和 qmake 构建决策，但 head 与 `current.md` 没有差异。PR 描述还明确写着“AGENTS.md / current.md changes kept local”。PR head 的 `current.md` 仍保留：

- `current.md:98-100`：Qt 构建系统和 admin-client 初始化仍是 TODO；
- `current.md:127`：`CMake versus qmake has not been decided`。

这同时违反仓库 `AGENTS.md` 的文档耦合要求、`CONTRIBUTING.md` 的项目文档要求，以及本组“所有代码/架构调整同步维护根目录 `current.md`”的约定。

**整改**：在 PR 中同步更新并压缩 `current.md`，记录 qmake 是否得到 A/B 正式确认、管理端骨架实际状态、未实现项、构建证据和后续依赖。若 qmake 尚未得到 A/B 确认，不得写成“已决策”。

### P1-02：C 专用追溯表与 A-S1-01 权威矩阵冲突

PR 新增的 `docs/requirements/README.md` 建立了另一套 `C-S1-001` 至 `C-S1-032` 编号。它与组内已冻结的 `docs/requirements/requirements-matrix.md` 不一致，例如：

- PR `docs/requirements/README.md:15` 把桩远程重启列为 C-S1-005/第一阶段；权威矩阵 `requirements-matrix.md:39` 明确远程重启是 P1、S2。
- PR `docs/requirements/README.md:16-17` 将站点管理、用户冻结/解冻整体列入第一阶段；权威矩阵 `requirements-matrix.md:40-41` 规定 S1 只做查询，新增站点和冻结/解冻属于 S2 操作。
- PR 的“已确认”状态容易被理解为已实现，但大量对应文件不存在或仍是“待实现”占位。

**整改**：以 A-S1-01 的 `requirements-matrix.md` 为唯一业务范围基线。C 的追溯表如需保留，应改为“C 任务映射/证据索引”，使用既定任务号 `C-S1-01`、`C-S1-02`、`C-S1-03`，不得重新定义阶段、优先级或验收范围。

### P1-03：系统架构文档错误地把智能分析写成扩展项

PR `docs/architecture/README.md:18` 写为“ml（智能分析，扩展项）”。本组权威需求已将智能分析固定为 S2 必做，包括 1/6/24 小时预测、低拥堵推荐、负荷预警和模型服务边界；它只是 S1 不依赖的能力，不是可选功能。

**整改**：改为“`ml/` 为第二阶段智能分析边界，S2 必做；不阻塞 S1 最小闭环”，并与 `current.md`、需求矩阵保持一致。

### P1-04：API 文档错误地把 S1 查询归入第二阶段

PR `docs/api/README.md:12-13` 将“站点查询”和“用户查询/冻结/解冻”整体写为第二阶段。权威矩阵规定：站点查询、站点详情和用户查询属于 S1；新增站点、冻结/解冻等写操作属于 S2。

**整改**：拆分为“S1 查询接口”和“S2 写操作接口”，并分别记录 B 的接口依赖与冻结状态。

## 4. 任务完成度判断

### 4.1 相对于 PR 标题 T-C0.1

作为“qmake 项目骨架、五页导航和最小启动冒烟”提交，PR 的方向基本成立：工程文件、主窗口、页面占位和登录锁定状态均已出现，且 `git diff --check` 通过。

但在 `current.md` 同步、构建系统正式确认和构建证据进入仓库前，不建议直接合并。

### 4.2 相对于 C-S1 管理端/大屏任务

不能判定完成。当前仅达到“骨架/进行中”：

- 登录按钮在 `apps/admin-client/src/pages/loginpage.cpp:36-38` 无条件发出 `loggedIn`，没有账号/密码校验、错误提示或服务不可用处理；提交者已在注释和 PR 描述中承认这是后续 TDD 内容。
- 概览、桩、站点、用户页只显示“待实现”提示。
- 没有 Repository 抽象和 Mock/Socket 双实现。
- C 追溯表列出的三组 Qt Test、大屏、接口闸门、回归和发布清单均不在 PR head 中。

这些缺失可以留给后续 C-S1 子任务，但必须在状态和 PR 描述中标为“未完成/后续任务”，不能把 C-S1 全体或 C-S1-001~032 当作已交付。

## 5. 次要问题与改进建议

### P2-01：README 使用个人绝对路径

`apps/admin-client/README.md:17-20、34-35` 和根 `README.md:42-44` 使用 `D:/Qt/...`、`D:/work/chargingplatform/...` 等个人机器路径。它们会误导其他成员和 Ubuntu 验收环境。

**建议**：使用相对路径、环境变量或明确的“示例路径”标注，并补充 qmake/qmake6 的版本检查命令。

### P2-02：构建/测试声明缺少仓库证据

PR 描述声称 clean rebuild、Qt smoke、启动验证均通过，但 PR 变更中没有构建日志、QtTest 输出、环境版本或可复现报告。当前环境也没有该 README 声明的 `D:/Qt` 工具链，因此不能独立复核。

**建议**：补充脱敏构建日志或 CI/Ubuntu 验证记录，至少包括 qmake 版本、构建命令、QtTest 完整输出、启动方式和提交 SHA；没有执行的检查保持“未验证”。

### P2-03：登录冒烟未覆盖真实按钮行为

现有 `tst_launchsmoke.cpp` 直接调用 `MainWindow::onLoginSuccess()` 和 `onLogout()`，没有点击登录按钮，也没有检查非法凭据不能进入业务区。对于骨架可以接受，但不能作为管理员认证功能的验收证据。

**建议**：后续增加 `tst_loginflow.cpp`，覆盖合法凭据、错误凭据、空输入、服务不可用和重复点击。

## 6. 已通过或未发现的问题

- PR head 和 base 可解析，文件范围集中在管理端和项目文档。
- qmake `subdirs`、源工程和测试工程的目录关系清晰，页面代码没有直接访问 SQL/SQLite。
- 页面与 Socket/数据库的架构边界说明方向正确。
- `.gitignore` 覆盖构建产物、运行数据库、`.env`；本次扫描没有发现真实腾讯地图 Key 或其它明显密钥。
- `git diff --check origin/main...origin/pr-2` 通过。

## 7. 最终结论

**结论：请求修改后再合并（Changes requested）。**

合并前最低要求：

1. 更新并压缩 `current.md`，解决 qmake 决策和 admin-client TODO 冲突。
2. 合并/改写 C 追溯文档，使其引用 A-S1-01 权威矩阵，不重新定义 S1/S2 范围。
3. 修正智能分析和站点/用户查询的阶段口径。
4. 在 PR 描述和文档中明确当前只是骨架，未把占位页、缺失 Repository、大屏和缺失测试写成已完成。
5. 后续 C-S1 实现 PR 再补齐真实/Mock 数据、错误态、三组专项测试、大屏和实际构建/联调证据。
