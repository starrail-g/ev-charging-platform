# 环境配置测试记录（2026-09-07 17:00–17:10）

> 官方节点（分工汇总 §3）：9 月 7 日 17:00–17:10 环境配置测试，全员完成，**C 负责记录**（Ubuntu、Qt、编译器版本和安装结果）。
> 更新规则：测试现场逐项填写后，把结果同步到课堂派提交记录并保留个人完成截图。

## 1. 测试环境（执行时填写实际值）

| 项 | 记录值 | 验证命令 |
|---|---|---|
| Ubuntu 版本/桌面 |  | `lsb_release -a` / `cat /etc/os-release` |
| Qt 版本 |  | `qmake6 -v` |
| C++ 编译器 |  | `g++ --version` |
| make/构建工具 |  | `make --version` |
| Python（脚本钩子用） |  | `python3 --version` |
| Node（Web 测试用） |  | `node --version` |
| 虚拟机工具（VMware 版本） |  | 宿主机 VMware 帮助/关于 |
| 网络/代理状态 |  | guest 内 `curl -I https://github.com`（被墙属预期，记录降级路径） |

## 2. 课堂派提交记录

- 提交时间：
- 提交结果（截图路径/文件名）：

## 3. 备注

- 与 18:00 接口闸门同日：本测试不阻塞闸门，但记录的环境版本是 9/10 干净环境构建声明的依据（C-S1-025）。
