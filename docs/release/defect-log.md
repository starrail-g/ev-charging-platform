# 缺陷日志（Defect Log）

> 编号规则：`C-S1-<三位序号>`（第一阶段），第二阶段使用 `C-S2-<三位序号>`。
> 状态：`待修复` / `修复中` / `已修复` / `已关闭（非缺陷）` / `延后`。
> 更新时机：每日结束固定动作 + 每次评审/测试发现缺陷时。

## 字段说明

| 字段 | 含义 |
|---|---|
| 编号 | C-S1-001 起，顺序递增，不复用 |
| 严重级别 | P0（阻塞构建/演示/核心功能）、P1（功能缺陷，有绕行）、P2（体验/文档小问题） |
| 模块 | 管理端 / 大屏 / 测试 / 文档 / 发布 |
| 描述 | 一句话缺陷 |
| 复现步骤 | 可复现的最小步骤 |
| 负责人 | 修复责任人 |
| 目标日期 | 计划修复日期 |
| 状态 | 见上 |

## 缺陷列表

| 编号 | 严重级别 | 模块 | 描述 | 复现步骤 | 负责人 | 目标日期 | 状态 |
|---|---|---|---|---|---|---|---|
| C-S1-001 | P0 | 数据库 | 迁移失败仍标 0.2 + 数据丢失风险（migration 无原子性） | 用 `build/repro/repro_migration.py`：trigger 校验失败后 schema_version 仍被改为 0.2；CHECK 失败场景合法数据全丢 | B | 9/4 前修复（合并前必须） | 已修复（T-B0.3 合入 origin/main；C 用 build/repro 复验通过后关闭，计划 9/5-6） |
| C-S1-002 | P1 | 协议 | 坏帧吞掉同批好帧（frame_codec feed 丢弃已解析消息） | 好帧+坏帧同批 feed：返回 0 条（对照组分两次 feed 返回 1 条）| B | Socket 适配层（9/6）前修复 | 已修复（T-B0.3 合入 origin/main；C 复验计划 9/5-6） |

## 关闭规则

- P0 必须当日修复并全量复测后才可标记 `已修复`。
- P1 仅在接口不变的前提下修复；P2 延后到第二阶段。
- `已关闭（非缺陷）` 必须写一行关闭原因。
